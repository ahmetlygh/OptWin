/**
 * Centralized DNS provider display names.
 * Used by ActionArea and PresetControls to avoid duplication.
 */
export const DNS_DISPLAY_NAMES: Record<string, string> = {
    default: "Default",
    cloudflare: "Cloudflare",
    google: "Google",
    opendns: "OpenDNS",
    quad9: "Quad9",
    adguard: "AdGuard",
};
